
all:
	cd ../src && pdflatex -output-directory=../build/ ./mabdelaz.tex
	cd ../src && pdflatex -output-directory=../build/ ./mabdelaz.tex
	cd ../src && pdflatex -output-directory=../build/ ./mabdelaz.tex

